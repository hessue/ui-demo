using UnityEngine;

namespace BlockAndDagger
{
    public class ToggleItemPanelController : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
